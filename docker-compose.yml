services:
  app:
    build: .
    container_name: coworking-gast-zugangssystem
    restart: unless-stopped
    volumes:
      - storage:/var/www/storage
    networks:
      - web

networks:
  web:
    external: true

volumes:
  storage:
